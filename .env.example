# Copy this file to `.env.local` for local development, and set the same
# values in Vercel → Project → Settings → Environment Variables for
# Preview + Production.

# --- Contact form email delivery (Resend) ---------------------------------
# 1. Create a free account at https://resend.com (sign up with the inbox
#    that should receive submissions so the shared test sender can reach it).
# 2. Dashboard → API Keys → create one, paste it here.
RESEND_API_KEY=

# Where contact submissions are delivered.
CONTACT_TO_EMAIL=oureditorialboard@gmail.com

# The "from" address. Leave as the Resend test sender until the domain is
# verified in Resend (Dashboard → Domains → add flourishchristianmagazine.org
# and add the DNS records). After that, switch to an address on the domain,
# e.g.  Flourish <contact@flourishchristianmagazine.org>
CONTACT_FROM_EMAIL=Flourish Contact Form <onboarding@resend.dev>

# --- Article submission ("Write for Flourish") email delivery (Resend) ----
# A SEPARATE Resend account from the one above — signed up under
# writeforflourishmagazine@gmail.com — because neither account's sending
# domain is verified yet, so each is sandboxed to only deliver to its own
# signup address (confirmed live). Falls back to RESEND_API_KEY above if
# this isn't set, but that account can't reach
# writeforflourishmagazine@gmail.com at all, so submissions need this key.
SUBMIT_ARTICLE_RESEND_API_KEY=

# Where submissions from the /write-for-flourish form are delivered.
SUBMIT_ARTICLE_TO_EMAIL=writeforflourishmagazine@gmail.com

# The "from" address for submission emails. Leave as the Resend test
# sender until this account's domain is verified in Resend.
SUBMIT_ARTICLE_FROM_EMAIL=Flourish Article Submissions <onboarding@resend.dev>
